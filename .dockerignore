**/bin/
**/obj/
out/
publish/
.vs/
.idea/
.vscode/
.git/
*.user
*.suo
# Docker dev files live in docker/ but the build context is the repo root,
# so exclude them from the build context to keep layer caching clean.
docker/compose*.yml
docker/.env*
.env
